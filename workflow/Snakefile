rule all:
    input:
        "",


rule pdftotext:
    input:
        "pdfs/{name}.pdf",
    output:
        "pdfs/pdftotext/{name}.txt",
    log:
        "logs/pdftotext/{name}.log",
    conda:
        "envs/pdftotext.yaml"
    shell:
        "pdftotext -layout -nodiag {input} {output}"
